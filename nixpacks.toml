[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs_20"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt", "cd apex/frontend && npm install"]

[phases.build]
cmds = ["cd apex/frontend && npm run build"]

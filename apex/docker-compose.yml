# NOTE: Use the docker-compose.yml in the project root instead.
#   cd .. && docker-compose up
#
# This file is kept for reference only. The root docker-compose.yml
# correctly resolves Python module paths (apex.backend.*) by using
# the repository root as the build context.

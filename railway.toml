[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONPATH=. alembic -c apex/backend/alembic.ini upgrade head && uvicorn apex.backend.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"

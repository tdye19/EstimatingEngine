[build]
dockerfilePath = "apex/backend/Dockerfile"

[deploy]
startCommand = "uvicorn apex.backend.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"

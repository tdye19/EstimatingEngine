{
  "name": "construction-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Construction market intelligence dashboard — BLS, FRED, Census data with MPI scoring engine",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static dashboard — no build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@upstash/redis": "^1.28.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vercel": "^33.0.0"
  }
}
